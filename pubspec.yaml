name: flsweep
description: The ultimate concurrent Flutter workspace cleaner and package syncer.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

executables:
  flsweep: flsweep

dependencies:
  args: ^2.4.2
  interact: ^2.2.0
  mason_logger: ^0.2.11
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
